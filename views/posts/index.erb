<!-- THIS IS A TEMPLATE BECAUSE IT IS USED ONLY ONCE AND GETS BIGGER -->
<!-- CAN THEN HAVE MULTIPLE PARTIALS -->
<h1>Welcome to the home page</h1>
<% @posts.each do |post| %>
  <div>
    <h3>
      <a href="<%= post[:id] %>"><%= post[:title] %></a>
      <p><%= post[:post_body] %></p>
    </h3>
  </div>
<% end %>
